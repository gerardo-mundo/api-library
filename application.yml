spring:
  application:
    name: api-library
  redis:
    host: localhost
    port: 6379
  cache:
    type: redis

security:
  jwt:
    secret-key: ${JWT_SECRET:YourFallbackSuperSecretKeyForLocalDevelopmentOnly}
    expiration-time: 86400000